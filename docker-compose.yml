services:
  backend:
    build:
      context: ./backend
    container_name: telcox-backend
    ports:
      - "5001:5000"
    environment:
      FLASK_ENV: development
      PYTHONPATH: /app
    volumes:
      - ./backend:/app